<form method="post" action="{{ path('app_pomodoros_delete', {'id': pomodoro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pomodoro.id) }}">
    <button class="btn btn-outline-danger">Delete 🗑</button>
</form>
