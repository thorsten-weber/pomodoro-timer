{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ app_name }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="content-header col-sm-12 text-center">
                <h1>{{ app_name }}</h1>
                <h2>{{ app_description }}</h2></div>
        </div>
    </div>

    {% include 'timer.html.twig' %}

    {% include 'tasks.html.twig' %}

{% endblock %}